export default class PMINetworkConfig {
    static baseURL = "https://cotsub.joinallofus.org/";
    static authentication = {
        login: "api/authenticate"
    };

    static donationSites = {
        search: "api/donationSite/search"
    };
}